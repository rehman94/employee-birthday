apiVersion: solution.sap/v1
kind: Solution

metadata:
  name: agent-discovery-4b5c6
  version: 3.0.0
  description: >
    An n8n workflow triggered by a webhook that lists all SAP agents available 
    for the tenant and invokes the Billing Posting Agent using the SAP Agent Node.

assets:
  - ref: ./assets/n8n/asset.yaml
